# Task: Revise `Performance comparison` (repaired run)

Source: `WRITEUP_100926.md` (review of `3_football_model_with_sqmc.tex`, section `\section{Performance comparison}`).
Guide: `dissertation/documents/TONE.md`.
Date: 2026-09-10.

## Scope

Revise the `\section{Performance comparison}` block of
`dissertation/drafts/chapters/3_football_model_with_sqmc.tex` (lines ~426–660) so
that every reported number, table, figure caption, and claim is consistent with
the **repaired authoritative run**, whose artifacts now live in
`dissertation/drafts/results/sqmc_ekf/`. Apply the tone rules in
`dissertation/documents/TONE.md` throughout.

**Do not** modify any pipeline code, and **do not** add the evaluation-only
checkpoint replay (deferred). Work only on the `.tex` chapter.

## Authoritative source of truth

All SQMC and EKF values below come from
`dissertation/drafts/results/sqmc_ekf/combined/results/performance_metrics.csv`,
`sqmc_predictions.json`, `ekf_predictions.json`, and
`combined/images/json/*_worldcup_rankings.json`. The EKF values are unchanged
from the old bundle; only SQMC values change. Do not invent numbers — read them
from these files.

### Confirmed authoritative values (repaired run)

- SQMC: 512 particles, 15 gradient replicas; final train log-likelihood
  `-1.465e4`; final test `-1.005e3`; best test `-9.600e2` at epoch 49; Brier
  `0.6878`; Brier skill vs uniform `-0.0316`; mean log-score `-3.664`; outcome
  accuracy `0.4712`; exact-score accuracy `0.0481`; 104 scored matches.
- EKF: Brier `0.5442`; skill `0.1838`; log-score `-3.131`; accuracy `0.5288`;
  exact `0.0769`; final train `-1.568e4`; final test `-1.040e3`; best test
  `-1.040e3` at epoch 50.
- Final match (Spain vs Argentina, 2026-07-19, actual 1–0):
  - EKF: home `0.4792`, draw `0.2069`, away `0.3139`, modal 1–1, log-lik `-2.565`.
  - SQMC: home `0.1463`, draw `0.3842`, away `0.4695`, modal 0–0, log-lik `-2.300`.
- SQMC pre-tournament top 10: Portugal, Belgium, England, France, Croatia,
  Japan, Czech Republic, Netherlands, Austria, Morocco.
- SQMC post-tournament top 10: Germany, Brazil, Sweden, Norway, England,
  Argentina, Australia, Ivory Coast, Canada, Netherlands.
- EKF pre-tournament top 10: Belgium, Brazil, Switzerland, Colombia, Austria,
  Portugal, France, Algeria, Ecuador, Japan (unchanged).
- EKF post-tournament top 10: Spain, Argentina, Brazil, Belgium, Portugal,
  Colombia, England, Morocco, Switzerland, France (unchanged).

## Edits to make, in order

1. **Experimental design (line ~439):** change "the four World Cup 2026
   fixtures used for the headline scoring" to "the 104 World Cup 2026 fixtures
   in the prediction period". Change "averaged over $10$ independently
   randomised RQMC replicas" to "$15$".
2. **Table `tab:logz`:** update SQMC final train to `-1.465e4`, best test to
   `-9.600e2` at epoch 49. Keep EKF as-is. Update the surrounding prose that
   cites `-1.499e4`, `-9.709e2` at epoch 26, and the "ten-replica gradient
   average" → "fifteen-replica".
3. **Brier equation (line ~482):** replace the inner sum `\sum_{j=1}^{N}` with
   a sum over the three outcomes, e.g. `\sum_{c\in\{H,D,A\}} (p_{ic}-o_{ic})^2`.
   Add one sentence that the sum is over the three outcomes and the uniform
   reference is `2/3`.
4. **Table `tab:prediction`:** update SQMC row to Brier `0.6878`, skill
   `-0.0316`, log-score `-3.664`, accuracy `0.4712`, exact `0.0481`. Keep EKF
   row and the `2/3` reference. Update the caption if it mentions the fixture
   count.
5. **Table `tab:pre-tournament`:** replace the SQMC column with the new top 10
   (Portugal, Belgium, England, France, Croatia, Japan, Czech Republic,
   Netherlands, Austria, Morocco). Keep EKF column.
6. **Spain–Argentina paragraph (line ~538):** rewrite so it states SQMC now
   assigns the highest probability to an **Argentina win (`0.4695`)**, then
   draw (`0.3842`), then Spain win (`0.1463`), with modal score 0–0; EKF
   assigns highest to a home win (`0.4792`) with modal 1–1. State explicitly
   these are pre-match predictive probabilities. Remove the old "0.537 home
   win" claim. Regenerate/verify the two score-grid figures still match the
   JSON.
7. **Table `tab:fifa-comparison`:** replace the SQMC post-tournament column
   with the new top 10 (Germany, Brazil, Sweden, Norway, England, Argentina,
   Australia, Ivory Coast, Canada, Netherlands). Keep EKF and FIFA columns.
8. **FIFA comparison paragraph (line ~636):** rewrite the RB-SQMC sentence. It
   now omits Spain, France, Portugal and Belgium from the top ten, places
   Sweden third, Norway fourth, Australia seventh, Ivory Coast eighth, Canada
   ninth, Netherlands tenth, and ranks Argentina sixth despite FIFA placing it
   second. Keep the EKF-alignment sentence.
9. **Timeseries paragraph (line ~577) and caption:** change "a representative
   team" to "the final top-five teams" and remove the claim that RB-SQMC
   reports "the particle spread as an uncertainty band" (the figure shows
   weighted posterior means only). Update the caption accordingly.
10. **Correlation figure (line ~595) and caption:** change the description
    from "correlation between the RB-SQMC top-$N$ rankings before and after the
    World Cup" to "final-state posterior correlations between team-strength
    coordinates". Remove all top-$N$ rank-stability wording.
11. **Discussion and Limitations (line ~640+):** soften "any difference
    between the methods is attributable to the correlated Rao-Blackwellised
    construction" to the defensible claim that both methods share the same
    frozen data split and observation model while differing in Gaussian vs
    particle approximation, optimisation noise, particle count, RQMC replica
    count, and time-step conventions. Change "not fully converged at $128$
    particles and $10$ gradient replicas" to "$512$ particles and $15$ gradient
    replicas", and frame the convergence claim as a single finite-particle
    realisation, not a demonstrated convergence. Update the runtime sentence if
    it cites stale compile/execute times (read from `performance_metrics.csv`).

## Constraints

- Keep the EKF column and all EKF prose unchanged.
- Do not add new sections or the eval-only replay.
- Every number must match the authoritative files.
- After editing, re-read the section to confirm no stale SQMC value remains:
  `0.6521`, `0.0218`, `-3.523`, `0.5096`, `0.0673`, `0.537`, `-9.709`,
  `-1.499`, `128`, `10`.

## Checklist

- [ ] 1. Experimental design: "four fixtures" → "104 fixtures"; replicas 10 → 15
- [ ] 2. `tab:logz` + prose: SQMC final train `-1.465e4`, best test `-9.600e2`
      (epoch 49), fifteen-replica wording
- [ ] 3. Brier equation: inner sum over outcomes `c∈{H,D,A}`; add 2/3 note
- [ ] 4. `tab:prediction`: SQMC row → `0.6878 / -0.0316 / -3.664 / 0.4712 / 0.0481`
- [ ] 5. `tab:pre-tournament`: SQMC column → new top 10
- [ ] 6. Spain–Argentina paragraph: SQMC favours Argentina win `0.4695`, modal
      0–0; EKF home `0.4792`, modal 1–1; label as pre-match
- [ ] 7. `tab:fifa-comparison`: SQMC post column → new top 10
- [ ] 8. FIFA paragraph: rewrite RB-SQMC sentence (omits
      Spain/France/Portugal/Belgium; Argentina 6th)
- [ ] 9. Timeseries: "final top-five teams", remove uncertainty-band claim
- [ ] 10. Correlation figure: "final-state posterior correlations between
       team-strength coordinates"
- [ ] 11. Discussion: soften attribution; 512 particles / 15 replicas;
       single-realisation framing; runtime from CSV
- [ ] 12. Final sweep: no stale SQMC values remain; EKF untouched
